# Скрипт для исправления электронного дневника

1. Перенесите `scripts.py` в папку с проектом (где лежит `manage.py`).
2. Запустите шелл: `python manage.py shell`.
3. Импортируйте функции: `from scripts import fix_marks, remove_chastisements, create_commendation`.
